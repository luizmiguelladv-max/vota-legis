<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ponto Eletrônico | Sistema de Ponto SaaS</title>
  <meta name="description" content="Sistema de ponto eletrônico SaaS para empresas. Reconhecimento facial, biometria, banco de horas, eSocial. Trial grátis por 14 dias.">
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --cor-primaria: #2563eb;
      --cor-secundaria: #1e40af;
      --cor-destaque: #10b981;
    }
    
    body {
      font-family: 'Inter', sans-serif;
      scroll-behavior: smooth;
    }
    
    .hero {
      background: linear-gradient(135deg, var(--cor-primaria) 0%, var(--cor-secundaria) 100%);
      min-height: 100vh;
      color: white;
      position: relative;
      overflow: hidden;
    }
    
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    }
    
    .navbar {
      background: transparent !important;
      transition: all 0.3s;
    }
    
    .navbar.scrolled {
      background: rgba(37, 99, 235, 0.95) !important;
      backdrop-filter: blur(10px);
    }
    
    .btn-cta {
      background: var(--cor-destaque);
      color: white;
      border: none;
      padding: 12px 32px;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s;
    }
    
    .btn-cta:hover {
      background: #059669;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    
    .feature-card {
      background: white;
      border-radius: 16px;
      padding: 32px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.05);
      transition: all 0.3s;
      height: 100%;
    }
    
    .feature-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    }
    
    .feature-icon {
      width: 64px;
      height: 64px;
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 28px;
      margin-bottom: 20px;
    }
    
    .pricing-card {
      background: white;
      border-radius: 24px;
      padding: 40px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.05);
      position: relative;
      transition: all 0.3s;
    }
    
    .pricing-card.destaque {
      border: 3px solid var(--cor-primaria);
      transform: scale(1.05);
    }
    
    .pricing-card.destaque::before {
      content: 'Mais Popular';
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--cor-primaria);
      color: white;
      padding: 4px 16px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
    }
    
    .pricing-price {
      font-size: 48px;
      font-weight: 700;
      color: var(--cor-primaria);
    }
    
    .pricing-price small {
      font-size: 16px;
      font-weight: 400;
      color: #6b7280;
    }
    
    .check-list li {
      list-style: none;
      padding: 8px 0;
      border-bottom: 1px solid #f0f0f0;
    }
    
    .check-list li i {
      color: var(--cor-destaque);
      margin-right: 8px;
    }
    
    .check-list li.disabled {
      color: #9ca3af;
      text-decoration: line-through;
    }
    
    .check-list li.disabled i {
      color: #d1d5db;
    }
    
    .cta-section {
      background: linear-gradient(135deg, var(--cor-secundaria) 0%, var(--cor-primaria) 100%);
      color: white;
      padding: 80px 0;
    }
    
    footer {
      background: #111827;
      color: #9ca3af;
      padding: 60px 0 30px;
    }
    
    @media (max-width: 768px) {
      .pricing-card.destaque {
        transform: scale(1);
      }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
      <a class="navbar-brand fw-bold fs-4" href="#">
        <i class="bi bi-clock-history me-2"></i>PontoSaaS
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto me-3">
          <li class="nav-item"><a class="nav-link" href="#recursos">Recursos</a></li>
          <li class="nav-item"><a class="nav-link" href="#planos">Planos</a></li>
          <li class="nav-item"><a class="nav-link" href="#contato">Contato</a></li>
        </ul>
        <a href="/login" class="btn btn-outline-light me-2">Entrar</a>
        <a href="#trial" class="btn btn-cta">Teste Grátis</a>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero d-flex align-items-center">
    <div class="container position-relative">
      <div class="row align-items-center">
        <div class="col-lg-6 mb-5 mb-lg-0">
          <h1 class="display-4 fw-bold mb-4">
            Sistema de Ponto Eletrônico <span class="text-warning">Completo</span>
          </h1>
          <p class="lead mb-4 opacity-75">
            Gerencie o ponto dos seus funcionários com reconhecimento facial, biometria digital, 
            banco de horas e integração eSocial. Tudo na nuvem.
          </p>
          <div class="d-flex flex-wrap gap-3">
            <a href="#trial" class="btn btn-cta btn-lg">
              <i class="bi bi-rocket-takeoff me-2"></i>Teste 14 Dias Grátis
            </a>
            <a href="#planos" class="btn btn-outline-light btn-lg">Ver Planos</a>
          </div>
          <div class="mt-4 d-flex gap-4 small opacity-75">
            <span><i class="bi bi-check-circle me-1"></i>Sem cartão de crédito</span>
            <span><i class="bi bi-check-circle me-1"></i>Setup em 5 minutos</span>
          </div>
        </div>
        <div class="col-lg-6 text-center">
          <img src="https://placehold.co/600x400/2563eb/white?text=Dashboard+Preview" 
               alt="Dashboard Preview" class="img-fluid rounded-4 shadow-lg">
        </div>
      </div>
    </div>
  </section>

  <!-- Recursos -->
  <section id="recursos" class="py-5" style="background: #f9fafb;">
    <div class="container py-5">
      <div class="text-center mb-5">
        <h2 class="display-6 fw-bold mb-3">Tudo que você precisa</h2>
        <p class="lead text-muted">Funcionalidades completas para gestão de ponto</p>
      </div>
      
      <div class="row g-4">
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-primary bg-opacity-10 text-primary">
              <i class="bi bi-camera-video"></i>
            </div>
            <h5>Reconhecimento Facial</h5>
            <p class="text-muted mb-0">Registro de ponto com câmera e inteligência artificial. Anti-fraude integrado.</p>
          </div>
        </div>
        
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-success bg-opacity-10 text-success">
              <i class="bi bi-fingerprint"></i>
            </div>
            <h5>Biometria Digital</h5>
            <p class="text-muted mb-0">Suporte a leitores biométricos Futronic, DigitalPersona e outros.</p>
          </div>
        </div>
        
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-warning bg-opacity-10 text-warning">
              <i class="bi bi-clock-history"></i>
            </div>
            <h5>Banco de Horas</h5>
            <p class="text-muted mb-0">Gestão completa de créditos, débitos e compensações automáticas.</p>
          </div>
        </div>
        
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-info bg-opacity-10 text-info">
              <i class="bi bi-building-check"></i>
            </div>
            <h5>Integração eSocial</h5>
            <p class="text-muted mb-0">Exportação de eventos S-1200, S-2230 e AFDT. Portaria 671.</p>
          </div>
        </div>
        
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-danger bg-opacity-10 text-danger">
              <i class="bi bi-bell"></i>
            </div>
            <h5>Notificações</h5>
            <p class="text-muted mb-0">Alertas de faltas, atrasos e banco de horas negativo.</p>
          </div>
        </div>
        
        <div class="col-md-6 col-lg-4">
          <div class="feature-card">
            <div class="feature-icon bg-secondary bg-opacity-10 text-secondary">
              <i class="bi bi-router"></i>
            </div>
            <h5>REP Control iD</h5>
            <p class="text-muted mb-0">Integração com relógios de ponto REP homologados.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Planos -->
  <section id="planos" class="py-5">
    <div class="container py-5">
      <div class="text-center mb-5">
        <h2 class="display-6 fw-bold mb-3">Planos que cabem no seu bolso</h2>
        <p class="lead text-muted">Escolha o plano ideal para sua empresa</p>
      </div>
      
      <div class="row g-4 justify-content-center" id="listaPlanos">
        <!-- Carregado via JavaScript -->
      </div>
    </div>
  </section>

  <!-- CTA Trial -->
  <section id="trial" class="cta-section">
    <div class="container text-center">
      <h2 class="display-5 fw-bold mb-4">Comece seu trial gratuito agora</h2>
      <p class="lead mb-4 opacity-75">14 dias grátis, sem cartão de crédito. Setup em 5 minutos.</p>
      
      <form id="formTrial" class="row g-3 justify-content-center" style="max-width: 600px; margin: 0 auto;">
        <div class="col-md-4">
          <input type="text" class="form-control form-control-lg" placeholder="Seu nome" name="nome" required>
        </div>
        <div class="col-md-4">
          <input type="email" class="form-control form-control-lg" placeholder="Email corporativo" name="email" required>
        </div>
        <div class="col-md-4">
          <button type="submit" class="btn btn-cta btn-lg w-100">Iniciar Trial</button>
        </div>
      </form>
    </div>
  </section>

  <!-- Contato -->
  <section id="contato" class="py-5" style="background: #f9fafb;">
    <div class="container py-5">
      <div class="row align-items-center">
        <div class="col-lg-6 mb-4 mb-lg-0">
          <h2 class="display-6 fw-bold mb-3">Fale com nosso time</h2>
          <p class="lead text-muted mb-4">Tire suas dúvidas ou solicite uma demonstração personalizada.</p>
          
          <div class="d-flex mb-3">
            <i class="bi bi-envelope fs-4 text-primary me-3"></i>
            <div>
              <h6 class="mb-0">Email</h6>
              <span class="text-muted">contato@pontosaas.com.br</span>
            </div>
          </div>
          
          <div class="d-flex mb-3">
            <i class="bi bi-whatsapp fs-4 text-success me-3"></i>
            <div>
              <h6 class="mb-0">WhatsApp</h6>
              <span class="text-muted">(11) 99999-9999</span>
            </div>
          </div>
        </div>
        
        <div class="col-lg-6">
          <div class="card border-0 shadow-sm">
            <div class="card-body p-4">
              <form id="formContato">
                <div class="mb-3">
                  <input type="text" class="form-control" placeholder="Nome completo" name="nome" required>
                </div>
                <div class="mb-3">
                  <input type="email" class="form-control" placeholder="Email" name="email" required>
                </div>
                <div class="mb-3">
                  <input type="tel" class="form-control" placeholder="Telefone" name="telefone">
                </div>
                <div class="mb-3">
                  <select class="form-select" name="funcionarios">
                    <option value="">Quantos funcionários?</option>
                    <option value="10">Até 10</option>
                    <option value="50">11 a 50</option>
                    <option value="200">51 a 200</option>
                    <option value="500">201 a 500</option>
                    <option value="1000">Mais de 500</option>
                  </select>
                </div>
                <div class="mb-3">
                  <textarea class="form-control" rows="3" placeholder="Mensagem" name="mensagem"></textarea>
                </div>
                <button type="submit" class="btn btn-primary w-100">Enviar Mensagem</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="container">
      <div class="row">
        <div class="col-lg-4 mb-4">
          <h5 class="text-white mb-3"><i class="bi bi-clock-history me-2"></i>PontoSaaS</h5>
          <p>Sistema de ponto eletrônico SaaS multi-tenant para empresas públicas e privadas.</p>
        </div>
        <div class="col-lg-2 mb-4">
          <h6 class="text-white mb-3">Produto</h6>
          <ul class="list-unstyled">
            <li><a href="#recursos" class="text-decoration-none text-muted">Recursos</a></li>
            <li><a href="#planos" class="text-decoration-none text-muted">Planos</a></li>
            <li><a href="#" class="text-decoration-none text-muted">Integrações</a></li>
          </ul>
        </div>
        <div class="col-lg-2 mb-4">
          <h6 class="text-white mb-3">Empresa</h6>
          <ul class="list-unstyled">
            <li><a href="#" class="text-decoration-none text-muted">Sobre</a></li>
            <li><a href="#contato" class="text-decoration-none text-muted">Contato</a></li>
            <li><a href="#" class="text-decoration-none text-muted">Blog</a></li>
          </ul>
        </div>
        <div class="col-lg-2 mb-4">
          <h6 class="text-white mb-3">Legal</h6>
          <ul class="list-unstyled">
            <li><a href="#" class="text-decoration-none text-muted">Privacidade</a></li>
            <li><a href="#" class="text-decoration-none text-muted">Termos</a></li>
          </ul>
        </div>
      </div>
      <hr class="my-4 opacity-10">
      <div class="text-center">
        <small>&copy; 2024 PontoSaaS. Todos os direitos reservados.</small>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Navbar scroll effect
    window.addEventListener('scroll', function() {
      const navbar = document.querySelector('.navbar');
      if (window.scrollY > 50) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
    });

    // Carrega planos
    async function carregarPlanos() {
      try {
        const res = await fetch('/api/planos');
        const planos = await res.json();
        
        const container = document.getElementById('listaPlanos');
        container.innerHTML = planos.map(p => `
          <div class="col-lg-3 col-md-6">
            <div class="pricing-card ${p.destaque ? 'destaque' : ''}">
              <h5 class="mb-2">${p.nome}</h5>
              <p class="text-muted mb-3">Até ${p.max_funcionarios === 99999 ? '∞' : p.max_funcionarios} funcionários</p>
              
              <div class="pricing-price mb-3">
                ${p.preco_mensal === 0 ? 'Sob consulta' : `R$${p.preco_mensal.toFixed(0)}<small>/mês</small>`}
              </div>
              
              <ul class="check-list ps-0 mb-4">
                <li class="${p.recursos.banco_horas ? '' : 'disabled'}">
                  <i class="bi bi-${p.recursos.banco_horas ? 'check-circle-fill' : 'x-circle'}"></i>
                  Banco de Horas
                </li>
                <li class="${p.recursos.afastamentos ? '' : 'disabled'}">
                  <i class="bi bi-${p.recursos.afastamentos ? 'check-circle-fill' : 'x-circle'}"></i>
                  Afastamentos
                </li>
                <li class="${p.recursos.esocial ? '' : 'disabled'}">
                  <i class="bi bi-${p.recursos.esocial ? 'check-circle-fill' : 'x-circle'}"></i>
                  eSocial
                </li>
                <li class="${p.recursos.app_mobile ? '' : 'disabled'}">
                  <i class="bi bi-${p.recursos.app_mobile ? 'check-circle-fill' : 'x-circle'}"></i>
                  App Mobile
                </li>
                <li class="${p.recursos.suporte_prioritario ? '' : 'disabled'}">
                  <i class="bi bi-${p.recursos.suporte_prioritario ? 'check-circle-fill' : 'x-circle'}"></i>
                  Suporte Prioritário
                </li>
              </ul>
              
              <a href="#trial" class="btn ${p.destaque ? 'btn-primary' : 'btn-outline-primary'} w-100">
                ${p.preco_mensal === 0 ? 'Falar com Vendas' : 'Começar Trial'}
              </a>
            </div>
          </div>
        `).join('');
      } catch (e) {
        console.error('Erro ao carregar planos:', e);
      }
    }

    // Form Trial
    document.getElementById('formTrial').addEventListener('submit', async function(e) {
      e.preventDefault();
      const formData = new FormData(this);
      
      try {
        await fetch('/api/leads', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            nome: formData.get('nome'),
            email: formData.get('email'),
            origem: 'landing_page_trial'
          })
        });
        
        alert('Obrigado! Você receberá um email com as instruções de acesso.');
        this.reset();
      } catch (e) {
        alert('Erro ao enviar. Tente novamente.');
      }
    });

    // Form Contato
    document.getElementById('formContato').addEventListener('submit', async function(e) {
      e.preventDefault();
      const formData = new FormData(this);
      
      try {
        await fetch('/api/leads', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            nome: formData.get('nome'),
            email: formData.get('email'),
            telefone: formData.get('telefone'),
            funcionarios: formData.get('funcionarios'),
            mensagem: formData.get('mensagem'),
            origem: 'landing_page_contato'
          })
        });
        
        alert('Mensagem enviada! Entraremos em contato em breve.');
        this.reset();
      } catch (e) {
        alert('Erro ao enviar. Tente novamente.');
      }
    });

    // Carrega planos ao iniciar
    carregarPlanos();
  </script>
</body>
</html>
