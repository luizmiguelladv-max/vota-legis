<!DOCTYPE html>
<html lang="pt-BR">
<head>
  @include('partials/head')
</head>
<body>
  <div class="d-flex">
    @include('partials/sidebar')

    <div class="main-content flex-grow-1">
      @include('partials/navbar')

      <main class="p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h4 class="section-title mb-0">
            <i class="bi bi-building me-2"></i>{{ municipio.nome }}
          </h4>
          <div>
            <a href="/admin/municipios/{{ municipio.id }}/editar" class="btn btn-outline-primary me-2">
              <i class="bi bi-pencil me-1"></i>Editar
            </a>
            <a href="/admin/municipios" class="btn btn-outline-secondary">
              <i class="bi bi-arrow-left me-1"></i>Voltar
            </a>
          </div>
        </div>

        <div class="row g-4">
          <div class="col-lg-8">
            <div class="card">
              <div class="card-header bg-white">
                <h6 class="mb-0">Informacoes</h6>
              </div>
              <div class="card-body">
                <div class="row g-3">
                  <div class="col-md-6">
                    <label class="text-muted small">Nome</label>
                    <p class="mb-0 fw-semibold">{{ municipio.nome }}</p>
                  </div>
                  <div class="col-md-3">
                    <label class="text-muted small">UF</label>
                    <p class="mb-0 fw-semibold">{{ municipio.uf }}</p>
                  </div>
                  <div class="col-md-3">
                    <label class="text-muted small">Status</label>
                    <p class="mb-0">
                      @if(municipio.ativo)
                        <span class="badge bg-success">Ativo</span>
                      @else
                        <span class="badge bg-danger">Inativo</span>
                      @end
                    </p>
                  </div>
                  <div class="col-md-4">
                    <label class="text-muted small">CNPJ</label>
                    <p class="mb-0">{{ municipio.cnpj || '-' }}</p>
                  </div>
                  <div class="col-md-4">
                    <label class="text-muted small">Telefone</label>
                    <p class="mb-0">{{ municipio.telefone || '-' }}</p>
                  </div>
                  <div class="col-md-4">
                    <label class="text-muted small">Email</label>
                    <p class="mb-0">{{ municipio.email || '-' }}</p>
                  </div>
                  <div class="col-12">
                    <label class="text-muted small">Endereco</label>
                    <p class="mb-0">{{ municipio.endereco || '-' }}</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="col-lg-4">
            <div class="card">
              <div class="card-header bg-white">
                <h6 class="mb-0">Status do Banco</h6>
              </div>
              <div class="card-body text-center">
                @if(municipio.bancoCriado)
                  <i class="bi bi-database-check text-success" style="font-size: 3rem;"></i>
                  <p class="mt-2 mb-0 text-success fw-semibold">Banco criado</p>
                @else
                  <i class="bi bi-database-x text-warning" style="font-size: 3rem;"></i>
                  <p class="mt-2 mb-3 text-warning">Banco pendente</p>
                  <form method="POST" action="/admin/municipios/{{ municipio.id }}/criar-banco">
                    {{ csrfField() }}
                    <button type="submit" class="btn btn-warning">
                      <i class="bi bi-database-add me-1"></i>Criar Banco
                    </button>
                  </form>
                @end
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  @include('partials/scripts')
</body>
</html>
