# Biometric API - Dependencias
# ==============================
# API universal para leitores biometricos (Futronic, DigitalPersona, ZKTeco, etc.)

# Framework web
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Validacao
pydantic==2.5.3

# Processamento de imagem (opcional, para preview)
Pillow==10.2.0
numpy==1.26.3

# CORS
python-multipart==0.0.6

# ============================================
# Dependencias por Sistema Operacional
# ============================================

# Windows - WMI para deteccao de dispositivos
# Instalar: pip install WMI pywin32
WMI==1.5.1; sys_platform == 'win32'
pywin32==306; sys_platform == 'win32'

# Linux - libusb para deteccao de dispositivos
# Instalar: pip install pyusb
# Nota: Pode precisar de: sudo apt-get install libusb-1.0-0-dev
pyusb==1.2.1; sys_platform == 'linux'

# macOS - nao precisa de dependencias extras (usa system_profiler)
# Mas pyusb pode ajudar em alguns casos
# pyusb==1.2.1; sys_platform == 'darwin'
