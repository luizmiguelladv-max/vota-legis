TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY=
NODE_ENV=development
SESSION_DRIVER=cookie
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# Data inicial para aceitar registros de ponto (formato: YYYY-MM-DD)
# Registros anteriores a esta data serão ignorados pelo webhook e sincronização
DATA_INICIAL_REGISTROS=

# DeepFace API (reconhecimento facial local)
# Microserviço Python com 99.5% de precisão (modelo ArcFace)
# Instalar: cd deepface-api && ./install.sh
DEEPFACE_URL=http://localhost:5000

# Azure Face API (reconhecimento facial em nuvem - backup)
# Requer aprovação especial da Microsoft
AZURE_FACE_ENDPOINT=https://seurecurso.cognitiveservices.azure.com/
AZURE_FACE_KEY=sua_chave_aqui

# Futronic API (leitor de digital USB)
# Microserviço Python para leitor Futronic FS80H
# Instalar: cd futronic-api && ./install.sh (Linux) ou install.bat (Windows)
FUTRONIC_URL=http://localhost:5001